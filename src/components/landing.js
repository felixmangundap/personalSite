import React from 'react'
import * as style from  '../styles/style.module.scss'

const Landing = () => (
  <div id='landing' className={`${style.landing} ${style.layer}`}>
    <div className={style.container}>
      <h1>hello, i'm felix</h1>
    </div>
  </div>
)

export default Landing
